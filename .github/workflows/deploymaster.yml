name: Java CI with Maven

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven
      - name: Build with Maven
        run: mvn -B package --file pom.xml

      - name: Delete files in server
        run: rm -rf /home/be8/be8-team4/shop/*

      - name: Copy files to server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SSH_NHN_IP }}
          username: ${{ secrets.SSH_NHN_ID }}
          port: ${{ secrets.SSH_NHN_PORT }}
          password: ${{ secrets.SSH_NHN_PASSWORD }}
          source: "target/*.jar"
          target: "/home/be8/be8-team4/shop"

      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_NHN_IP }}
          username: ${{ secrets.SSH_NHN_ID }}
          port: ${{ secrets.SSH_NHN_PORT }}
          password: ${{ secrets.SSH_NHN_PASSWORD }}
          script: |
            mkdir -p /home/be8/be8-team4/shop
            
            cd /home/be8/be8-team4/shop
            
            echo -e "Starting jar on port 10143"
            nohup /usr/local/java/java21/bin/java -Dserver.port=10143 -Dspring.profiles.active=nhn -jar /home/be8/be8-team4/shop/target/book-0.0.1-SNAPSHOT.jar > /home/be8/be8-team4/shop/log_10143.log 2>&1 &

            echo -e "Starting jar on port 10144"
            nohup /usr/local/java/java21/bin/java -Dserver.port=10144 -Dspring.profiles.active=nhn -jar /home/be8/be8-team4/shop/target/book-0.0.1-SNAPSHOT.jar > /home/be8/be8-team4/shop/log_10143.log 2>&1 &